# Site README

Title, one paragraph (origin and stack), optional Development (`bun install` / `bun run dev`) in `docs/development.mdx`, license-follows-disk, footer. Product protocol essays are not the site README.
