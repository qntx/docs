# CLI README (postcard)

Root `README.md` stays the library postcard. **Deferred** (do not put these on the GitHub README until a later decision): `demo.gif`, `curl | sh`, PowerShell `irm …/ps | iex`, and a usage command.

Those belong in `docs/install.mdx` and `docs/index.mdx`:

```bash
curl -fsSL https://sh.qntx.org/<repo> | sh
```

The postcard still uses `See [docs/](docs/).` so GitHub readers find install.
