---
title: Overview
description: These pages are fixtures for layout QA, not product documentation.
---

This site ships a **fixture tree**. The chrome (nav, search, Copy Markdown, Open in, `llms.txt`) is real. The product copy is not.

Use the tree to confirm:

- Root tabs switch between Getting Started, Writing, Reference, and API.
- Article pages render a title, description, Copy Markdown, and Open in.
- Internal links stay inside the app; external links open a new tab.

## Next

- [Quickstart](./quickstart.mdx) — install the example CLI.
- [Typography](/writing/typography#custom-anchor) — jump to a custom heading id.
- [Fumadocs markdown](https://www.fumadocs.dev/docs/markdown) — upstream syntax this kit follows.

<Callout title="Not product docs">
  Nothing under `/start`, `/writing`, `/reference`, or `/api` describes a shipped QuantX protocol.
</Callout>
